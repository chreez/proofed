---
id: PF-256.4
title: Slice 1 — Pricing rework on ProductionPlan (/pricing v2)
status: To Do
assignee: []
created_date: '2026-05-14 21:05'
labels:
  - bakery-ops
  - slice-1
  - pricing
  - rework
dependencies: []
parent_task_id: PF-256
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Rework /pricing to read from ProductionPlan as source of truth. Replaces the original PF-255.1 pricing MVP (user flagged 'not shippable'). Folds DRAFT-90 (units toggle + opt-in) — opt-in semantics are now the cart itself; per-unit math flows from ProductionEntry.unit + baseYield. Whole-dollar pricing only (no .99/.95 snap).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 /pricing route remains; PricingView.vue replaced with a ProductionPlan-aware view.
- [ ] #2 Empty state: when ProductionPlan.entries is empty, CTA card linking back to /production.
- [ ] #3 Each cart entry renders as a pricing row: recipe name + qty (batches × unit count, e.g. '1 batch · 8 rolls'), cost per batch, cost per unit, markup% slider (50-300, step 1, default 150), live sell price per unit (whole-dollar rounded), live CP$ per unit + CP%.
- [ ] #4 Cost resolution per recipe: prefer most-recent cook_log[].cost.totalCost; fall back to recipe.estimatedCost.total. If neither, show 'no cost data' inline (don't block render).
- [ ] #5 Per-unit math: costPerUnit = totalBatchCost / (batches × baseYield) when no override; override uses yieldOverride directly.
- [ ] #6 Whole-dollar snap (replaces PF-255.1's psych-pricing breakpoints): round sellPrice to nearest dollar. Display always shows '' not '.99'.
- [ ] #7 Header summary row: '{N} bakes queued · total cost  · total revenue  · total CP '.
- [ ] #8 localStorage 'bake-pricing-current' stores per-recipe markupPct overrides (separate from ProductionPlan). Existing PricingProfile localStorage data is either migrated or cleared on first load — no shippable state from PF-255.1 to preserve.
- [ ] #9 Dense data-driven layout matching /production library row style (compact, scannable, not card-heavy).
- [ ] #10 F43 tooltips on slider, sell price, CP%, per-unit math hints — uses HelpTooltip primitive.
- [ ] #11 F27 useSeoMeta with 'Pricing · proofed.'
- [ ] #12 Mobile-responsive single-column stack at <=640px.
- [ ] #13 Snapshot test for the pricing row component.
- [ ] #14 Old PricingRow.vue + usePrettyPrice.ts + usePricingProfile.ts removed (or rewritten) — PF-255.1 work no longer reachable. PROJECT_STATUS notes the supersession.
- [ ] #15 npm run build passes; diff-coverage >=90% lines / >=80% branches on new files.
<!-- AC:END -->
