---
id: PF-118.1
title: Schema spike — StateNote.source field
status: To Do
assignee: []
created_date: '2026-02-11 19:41'
labels:
  - spike
  - schema
dependencies: []
parent_task_id: PF-118
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Add a `source` field to the `StateNote` type in `src/types/recipe.ts`. Values: `'user' | 'agent'`. This is the data-layer foundation for distinguishing user-authored notes from agent-contributed content (tips, definitions, technique explanations) in recipe JSON.

**Scope (spike — data layer only, no rendering changes):**
- Add `source?: 'user' | 'agent'` to the `StateNote` interface in `recipe.ts`
- Backfill all existing `step_notes` entries in `public/recipes/*.json` with `"source": "user"` (all current notes are user-authored)
- Update any validation checks that touch StateNote shape (checklist.md if needed)
- Document the changelog entry convention: when agent adds notes during a `/feedback` session, the version bump changelog should mention "agent-sourced notes added" or similar
- Verify `npm run build` passes with the type change + backfilled data
- Investigate whether `CookLogEntry.step_notes` (currently `Record<string, string>`) needs restructuring to carry source attribution — document recommendation in PF-118 implementation notes

**Context:** This field enables the demo spike (rendering differentiation) and the /feedback skill (writes agent-sourced notes). Schema change must land first.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 `StateNote` interface in `src/types/recipe.ts` includes `source?: 'user' | 'agent'`
- [ ] #2 All existing `StateNote` entries in `public/recipes/*.json` backfilled with `"source": "user"` — no entry left without a source value
- [ ] #3 Spike documents findings on whether `CookLogEntry.step_notes` (currently `Record<string, string>`) needs restructuring to carry source attribution — recommendation captured in parent task (PF-118) implementation notes
- [ ] #4 Changelog convention documented: feedback-session version bumps note agent-sourced additions (e.g. "Added agent-sourced technique notes to RISE_1, ADD_BUTTER")
- [ ] #5 Validation checklist updated if any new checks are needed for the `source` field
- [ ] #6 `npm run build` passes with the type change and backfilled data
- [ ] #7 No rendering changes — this spike is data layer only
<!-- AC:END -->
