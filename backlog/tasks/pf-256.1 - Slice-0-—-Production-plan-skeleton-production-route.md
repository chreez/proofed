---
id: PF-256.1
title: Slice 0 — Production plan skeleton (/production route)
status: To Do
assignee: []
created_date: '2026-05-13 01:39'
labels:
  - bakery-ops
  - slice-0
  - foundation
dependencies: []
parent_task_id: PF-256
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
First shippable child of PF-256 Bakery Ops Assistant epic. Establishes the shared ProductionPlan localStorage state + /production route + UI mutation. Every later lens (price, labels, schedule, sell, financials, data health) reads from this state. No prices, dates, status, or schedule yet — pure intent capture with provenance.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 New flat route /production registered in src/router/index.ts (matches existing flat pattern).
- [ ] #2 Route is public; no auth gate.
- [ ] #3 New TS types in src/types/production.ts: ProductionEntry = { id, recipeId, quantity, unit, addedBy, addedAt }; ProductionPlan = { entries: ProductionEntry[], updated: string }. addedBy: 'user' | 'agent' mirrors StateNote.source convention.
- [ ] #4 New composable src/composables/useProductionPlan.ts exports: loadPlan(), savePlan(), addEntry(), removeEntry(), updateEntry(). Working state in localStorage['bake-production-current'].
- [ ] #5 New view src/components/ProductionView.vue rendered via App.vue route-meta dispatch (matches PricingView/StatsView pattern).
- [ ] #6 View renders: header + [+ Add to production] button + entries list.
- [ ] #7 Add flow: button opens recipe picker (lists every recipe from public/recipes/index.json); selecting writes new entry with addedBy 'user', addedAt new Date().toISOString(), quantity 1, unit defaulted from recipe meta (best-effort parse of meta.yields, else 'unit').
- [ ] #8 Remove flow: inline [x] button on each row; confirms before removing.
- [ ] #9 Edit flow: inline number input on quantity, inline text input on unit. Changes persist immediately.
- [ ] #10 Provenance display: each row shows 'added by {addedBy} · {addedAt as locale date}' in small muted text.
- [ ] #11 Empty state: when entries.length === 0, renders 'No bakes in production. [+ Add your first]' CTA centered.
- [ ] #12 Every interactive element (Add button, Remove button, qty input, unit input, recipe picker rows) has a help tooltip on hover/focus (F43).
- [ ] #13 F27: view calls useSeoMeta with route-specific title ('Production · proofed.') and description.
- [ ] #14 Mobile-responsive (single-column stack at <640px); snapshot test in src/components/ProductionRow.spec.ts; npm run build passes.
<!-- AC:END -->
