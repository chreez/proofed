---
id: PF-211.9
title: 'Implement: Cost breakdown component'
status: To Do
assignee: []
created_date: '2026-04-22 05:15'
updated_date: '2026-04-22 05:28'
labels:
  - feature
dependencies: []
parent_task_id: PF-211
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Simplified cost display on print page. Two numbers: total bake cost + per-serving cost.

Data source: average cost across all bakes on the current **major version** (vX.*). If no bakes exist on the current major version with cost data, cost section does not render at all (no placeholder).

Example: recipe at v3.6.0 → average cost of all cook_log entries where version starts with "v3." and has cost data.

CostBreakdown.vue component already exists but is over-engineered for this use case. Print view needs inline rendering, not the full component.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] Composable `getVersionAverageCost(recipe)` returns `{ total: number, perServing: number } | null`
- [ ] Filters cook_log entries by current major version (vX.*)
- [ ] Averages `cost.total` and `cost.perServing` across matching entries
- [ ] Returns null when no matching entries have cost data
- [ ] Print view renders cost inline (not full CostBreakdown component) — two values only
- [ ] Cost section hidden (not rendered) when `getVersionAverageCost` returns null
- [ ] Unit tests: multiple bakes same major version, cross-version filtering, no cost data
- [ ] npm run build passes
<!-- AC:END -->
