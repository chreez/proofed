---
id: PF-256.5
title: Slice 6 — Sell lens (/sales route)
status: To Do
assignee: []
created_date: '2026-05-16 00:10'
labels:
  - bakery-ops
  - slice-6
  - sales
  - ui
dependencies: []
parent_task_id: PF-256
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
S6 slice of PF-256 Bakery Ops Assistant. Closes the farmers-market workflow loop. /sales lens consumes ProductionPlan + /pricing state to track per-recipe sold-vs-planned during a market session, surfaces end-of-day cash totals + leftover inventory, archives closed sessions to a local history.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 New flat route /sales registered in src/router/index.ts; public, no auth gate.
- [x] #2 App.vue dispatches LabelsView/PricingView pattern; SalesView.vue rendered via route-meta.
- [x] #3 Data model: MarketSession { id, openedAt, closedAt?, label?, sales: SaleEntry[] } and SaleEntry { recipeId, plannedUnits, soldUnits, unitPrice, notes? } in src/types/sales.ts.
- [x] #4 Composable src/composables/useSales.ts exports: loadActiveSession(), openSession({fromPlan, pricing}), recordSale(session, recipeId, units), closeSession(session), loadHistory(). localStorage keys 'bake-sales-active' (single active) + 'bake-sales-history' (array of closed sessions, capped at 50 most recent).
- [x] #5 Open Session flow: 'Start market session' button on empty state snapshots current ProductionPlan entries + pricing (sellPrice per unit from /pricing) into a new MarketSession. Each plan entry becomes a SaleEntry with plannedUnits = unitsPerBake, soldUnits = 0, unitPrice = current sell price.
- [x] #6 Per-row UI during active session: recipe name + thumb + 'planned: M' label + sold stepper input (range 0..M, step 1) + revenue ( = sold × unitPrice) + leftover (M - sold). Sold stepper accepts overshoot via direct typing (user can record sales > planned if they restocked mid-market, with confirm).
- [x] #7 Session header summary: 'Active session · {label} · {N} recipes · sold {X} of {Y} · '. Sticky on scroll.
- [x] #8 Close Session flow: 'Close session' button confirms then writes session to history with closedAt timestamp. Clears bake-sales-active. Shows brief confirmation toast/banner.
- [x] #9 History list (below active session OR shown when no active session): scrollable list of closed sessions, newest first, showing date + label + total revenue + count of recipes. Each entry clickable to expand inline breakdown.
- [x] #10 Empty state (no active + no history): centered CTA with 'No market session yet. Start one to track sales from your production plan.' + Start button. If /production plan is empty, CTA links to /production first.
- [x] #11 F43 tooltips on all interactive elements via HelpTooltip primitive.
- [x] #12 F27 useSeoMeta with 'Sales · proofed.'
- [x] #13 Mobile-responsive single-column at <=640px; sold stepper buttons sized for touch.
- [x] #14 Snapshot test for SalesRow component (active session + closed history row variants).
- [x] #15 Build passes; diff-coverage ≥90% lines / ≥80% branches on new files.
<!-- AC:END -->
