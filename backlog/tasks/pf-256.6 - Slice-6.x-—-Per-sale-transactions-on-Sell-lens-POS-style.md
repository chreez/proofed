---
id: PF-256.6
title: Slice 6.x — Per-sale transactions on Sell lens (POS-style)
status: To Do
assignee: []
created_date: '2026-05-16 00:24'
labels:
  - bakery-ops
  - slice-6
  - sales
  - pos
dependencies: []
parent_task_id: PF-256
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Reshape PF-256.5 Sell lens from aggregate sold-per-recipe to per-transaction tracking. Each customer interaction = one SalesTransaction with line items. Total ask computed live during build. Session totals derive from transactions. Drill-down per recipe shows all transactions including it.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Data model in src/types/sales.ts: MarketSession gains 'transactions: SalesTransaction[]' and 'initialPlan: PlanSnapshot[]' (PlanSnapshot = { recipeId, plannedUnits, unitPrice }); legacy 'sales: SaleEntry[]' deprecated but readable on closed sessions for backwards compat.
- [ ] #2 New types: SalesTransaction = { id, occurredAt, items: TransactionItem[], totalAsk, notes? }; TransactionItem = { recipeId, units, unitPrice, lineTotal }.
- [ ] #3 useSales composable adds: recordTransaction(session, items[]) → returns updated session with new transaction appended; deleteTransaction(session, txId) → returns session without it (basic undo); aggregateSold(session, recipeId) → derives count from transactions.
- [ ] #4 SalesView: 'New Sale' button prominent in toolbar (was 'Start Session' before — Start Session opens the session as before, then New Sale records customer transactions within).
- [ ] #5 New Sale sheet: opens as bottom sheet (mobile) or modal (desktop). Lists all recipes in initialPlan with thumb, +/- stepper to build cart, live 'Total: ' at bottom, default unitPrice from snapshot but editable per-item (discount or override).
- [ ] #6 Complete Sale button on sheet → calls recordTransaction → sheet closes → row added to transaction log.
- [ ] #7 Transaction log: chronological list below the toolbar (newest first). Each row: time + item count + total. Click to expand inline showing items + delete button.
- [ ] #8 Session header summary updates from transactions: 'sales count: N · sold: X items · revenue: '.
- [ ] #9 Aggregate sold per recipe shown in a collapsed 'By recipe' view: click to expand → per-recipe row with total sold across all transactions + leftover from plannedUnits.
- [ ] #10 Migration: closed sessions from PF-256.5 (with sales: SaleEntry[]) render in history with their original aggregate display; new sessions write transactions. No destructive migration.
- [ ] #11 F43 tooltips on all interactive elements via HelpTooltip primitive.
- [ ] #12 F27 useSeoMeta preserved.
- [ ] #13 Mobile-responsive; bottom sheet sized 80vh + touch-friendly stepper buttons.
- [ ] #14 Snapshot test for SalesTransactionRow (expanded + collapsed) + NewSaleSheet (empty + populated cart).
- [ ] #15 Build passes; diff-coverage ≥90% lines / ≥80% branches on new/changed files.
<!-- AC:END -->
