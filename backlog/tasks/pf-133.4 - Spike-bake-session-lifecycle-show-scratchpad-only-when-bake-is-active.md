---
id: PF-133.4
title: 'Spike: bake session lifecycle - show scratchpad only when bake is active'
status: To Do
assignee: []
created_date: '2026-02-14 21:59'
updated_date: '2026-02-14 22:05'
labels:
  - spike
dependencies: []
parent_task_id: PF-133
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Scratchpad icons, FAB, and reminder banners currently show on every recipe page load. They should only appear after the user explicitly starts a bake session.

**Questions to explore:**
- What triggers "bake started"? (First checkbox? Explicit "Start Bake" button? Auto-detect from progress?)
- Where does the start bake CTA live? (Hero area? Sticky header? First stage?)
- What state changes when a bake is active vs browsing? (scratchpad visible, progress tracking active, timer behavior?)
- How does a bake session end? (Explicit "End Bake"? Auto after last step? Timeout?)
- Does bake session state persist across page reloads? (localStorage alongside scratchpad data)
- How does this interact with the existing progress composable?

**Context:** Right now the recipe page is both a reference view (browsing) and a bake view (active cooking). This spike explores making that distinction explicit so the UI can adapt — scratchpad, reminders, and capture tools only surface when you're actually baking.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Document 3+ trigger options for Start Bake (explicit button, first checkbox, auto-detect from progress) with pros/cons and impact on useProgress/useScratchpad
- [ ] #2 Document 2+ end-session mechanisms (explicit End Bake, auto after last stage) and what happens to scratchpad data on session end
- [ ] #3 Map all bake-mode UI surface area -- list every component/element that hides in browse mode vs shows in bake mode with specific file references
- [ ] #4 Evaluate persistence strategy for bake-session state (localStorage key naming alongside existing recipe-progress and scratchpad keys, new composable vs fold into useProgress)
- [ ] #5 Address returning-to-in-progress-bake scenario -- describe UX for page reload mid-bake (auto-resume, banner prompt, or re-click)
- [ ] #6 Evaluate impact on shared-mode/QR viewers -- confirm browse mode is default for non-owner viewers
- [ ] #7 Written recommendation (trigger, lifecycle, persistence, UX) documented in PF-133 implementation notes
<!-- AC:END -->
